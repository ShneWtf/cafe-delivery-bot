# 🚀 Быстрый старт

## Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

## Шаг 2: Создание .env файла

Создайте файл `.env` в корне проекта со следующим содержимым:

```env
BOT_TOKEN=your_bot_token_from_botfather
TELEGRAM_API_TOKEN=your_telegram_api_token
WEBAPP_URL=https://your-domain.com/app/
YOOKASSA_SHOP_ID=
YOOKASSA_SECRET_KEY=
DIRECTOR_ID=7592151419
ADMIN_IDS=
```

**Где получить токены:**
- `BOT_TOKEN` - получите у [@BotFather](https://t.me/BotFather) командой `/newbot`
- `TELEGRAM_API_TOKEN` - получите на [my.telegram.org](https://my.telegram.org)
- `WEBAPP_URL` - URL вашего Web App (для локальной разработки используйте ngrok)

## Шаг 3: Запуск бота

```bash
python bot/main.py
```

При первом запуске автоматически:
- ✅ Создается база данных `bot.db`
- ✅ Создается директор с ID: 7592151419

## Шаг 4: Запуск Web App backend

В отдельном терминале:

```bash
cd webapp
uvicorn api:app --host 0.0.0.0 --port 8000
```

## Шаг 5: Настройка Web App в BotFather

1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/newapp`
3. Выберите вашего бота
4. Укажите название и описание
5. Укажите URL: `https://your-domain.com/app/` (или ngrok URL)
6. Сохраните

## Шаг 6: Заполнение меню (опционально)

```bash
python bot/init_sample_data.py
```

## ✅ Готово!

Теперь:
1. Откройте бота в Telegram
2. Отправьте `/start` - получите приветственные бонусы
3. Нажмите "Открыть меню" - откроется Web App
4. Если вы директор (ID: 7592151419), используйте `/director` для управления ролями

## 🔧 Локальная разработка с ngrok

Для локальной разработки Web App используйте ngrok:

```bash
# Установите ngrok
# Запустите Web App backend на порту 8000
# В другом терминале:
ngrok http 8000

# Скопируйте HTTPS URL (например: https://abc123.ngrok.io)
# Используйте его в WEBAPP_URL и в BotFather
```

## 📝 Команды для тестирования

- `/start` - начать работу (получить бонусы)
- `/director` - панель директора (только ID: 7592151419)
- `/admin` - панель админа (после добавления через /director)
- `/courier` - панель курьера (после добавления через /director)

## 🐛 Решение проблем

**Бот не запускается:**
- Проверьте, что `BOT_TOKEN` указан в `.env`
- Убедитесь, что все зависимости установлены

**Web App не открывается:**
- Проверьте, что Web App backend запущен
- Убедитесь, что URL в BotFather правильный
- Telegram требует HTTPS - используйте ngrok или сервер с SSL

**База данных не создается:**
- Проверьте права на запись в директории проекта
- Убедитесь, что `bot.db` не заблокирован другим процессом
